# WooOmni Module
# Player data modules use database storage (sqlite/mysql)
# Config modules always use YAML

modules:
  fly:
    enabled: true
    
  god:
    enabled: true
    
  vanish:
    enabled: true
